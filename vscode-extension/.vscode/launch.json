{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ApexFlow Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}
